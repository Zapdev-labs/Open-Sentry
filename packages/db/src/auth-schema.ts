// Auth tables removed — Clerk manages users, sessions, organizations, and memberships externally.
// Projects reference Clerk org IDs via the `organizationId` text field (no FK constraint).
export {};
